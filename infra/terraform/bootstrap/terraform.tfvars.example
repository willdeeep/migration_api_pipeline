# Copy to terraform.tfvars and fill in (terraform.tfvars is gitignored).
project_id        = "migration-api-pipeline"
region            = "us-central1"
state_bucket_name = "migration-api-pipeline-tfstate" # must be globally unique
github_repository = "willdeeep/migration_api_pipeline"
